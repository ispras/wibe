algorithms:
  NormSpace:
    params:
      sample_rate: 16000
      watermark_length: 40
      delta: 0.03
      wavelet: db1

post_embed_metrics:
{% include 'common/post_embed_metrics.yml' %}

post_extract_metrics:
{% include 'common/post_extract_metrics.yml' %}

datasets:
{% include 'common/librispeech.yml' %}

attacks:
{% include 'common/distortions.yml' %}

pipeline:
  result_path: ./test_results/norm_space
  aggregators:
    - CSV:
        table_name: table
