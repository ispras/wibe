algorithms:
  ringid

datasets:
  PromptFolderDataset:
    path: tests/data

attacks:
  Identity

pipeline:
  result_path: ./test_results/tests/ringid
  aggregators:
    - CSV:
        table_name: table
