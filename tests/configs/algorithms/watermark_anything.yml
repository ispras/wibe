algorithms:
  watermark_anything

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  Identity

pipeline:
  result_path: ./test_results/tests/watermark_anything
  aggregators:
    - CSV:
        table_name: table
