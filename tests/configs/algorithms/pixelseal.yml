algorithms:
  pixelseal

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  Identity

pipeline:
  result_path: ./test_results/tests/pixelseal
  aggregators:
    - CSV:
        table_name: table
