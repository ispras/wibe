algorithms:
  invismark

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  Identity

pipeline:
  result_path: ./test_results/tests/invismark
  aggregators:
    - CSV:
        table_name: table
