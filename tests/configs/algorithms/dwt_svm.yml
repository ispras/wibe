algorithms:
  dwt_svm

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  Identity

pipeline:
  result_path: ./test_results/tests/dwt_svm
  aggregators:
    - CSV:
        table_name: table
