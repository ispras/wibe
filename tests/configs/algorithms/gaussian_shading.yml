algorithms:
  gaussian_shading

datasets:
  PromptFolderDataset:
    path: tests/data

attacks:
  Identity

pipeline:
  result_path: ./test_results/tests/gaussian_shading
  aggregators:
    - CSV:
        table_name: table
