algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  nrpsmall

pipeline:
  result_path: ./test_results/tests/nrpsmall
  aggregators:
    - CSV:
        table_name: table
