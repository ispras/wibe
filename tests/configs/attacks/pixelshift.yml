algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  pixelshift

pipeline:
  result_path: ./test_results/tests/pixelshift
  aggregators:
    - CSV:
        table_name: table
