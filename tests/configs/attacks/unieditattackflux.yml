algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  UniEditAttackFlux:
    num_steps: 1

pipeline:
  result_path: ./test_results/tests/unieditattackflux
  aggregators:
    - CSV:
        table_name: table
