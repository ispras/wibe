algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  wpwmattacker

pipeline:
  result_path: ./test_results/tests/wpmattacker
  aggregators:
    - CSV:
        table_name: table
