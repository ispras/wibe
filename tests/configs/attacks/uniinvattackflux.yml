algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  UniInvAttackFlux:
    num_steps: 1

pipeline:
  result_path: ./test_results/tests/uniinvattackflux
  aggregators:
    - CSV:
        table_name: table
