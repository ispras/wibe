algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  rotate90

pipeline:
  result_path: ./test_results/tests/rotate90
  aggregators:
    - CSV:
        table_name: table
