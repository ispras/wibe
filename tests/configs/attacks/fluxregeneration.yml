algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  fluxregeneration:
    num_inference_steps: 1

pipeline:
  result_path: ./test_results/tests/fluxregeneration
  aggregators:
    - CSV:
        table_name: table
