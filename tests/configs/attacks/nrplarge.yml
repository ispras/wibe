algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  nrplarge

pipeline:
  result_path: ./test_results/tests/nrplarge
  aggregators:
    - CSV:
        table_name: table
