algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  dogblurdeblurfpninception

pipeline:
  result_path: ./test_results/tests/dogblurdeblurfpninception
  aggregators:
    - CSV:
        table_name: table
