algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  semattack:
    num_inference_steps_attacker: 1
    steps: 1

pipeline:
  result_path: ./test_results/tests/semattack
  aggregators:
    - CSV:
        table_name: table
