algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  fluxrinsing:
    num_inference_steps: 1
    rinsing_times: 1

pipeline:
  result_path: ./test_results/tests/fluxrinsing
  aggregators:
    - CSV:
        table_name: table
