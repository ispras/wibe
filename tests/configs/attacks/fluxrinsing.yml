algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  fluxrinsing

pipeline:
  result_path: ./test_results/tests/fluxrinsing
  aggregators:
    - CSV:
        table_name: table
