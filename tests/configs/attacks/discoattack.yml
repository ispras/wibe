algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  discoattack

pipeline:
  result_path: ./test_results/tests/discoattack
  aggregators:
    - CSV:
        table_name: table
