algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  imagewatermark

pipeline:
  result_path: ./test_results/tests/imagewatermark
  aggregators:
    - CSV:
        table_name: table
