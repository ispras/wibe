algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  diffpureattack

pipeline:
  result_path: ./test_results/tests/diffpureattack
  aggregators:
    - CSV:
        table_name: table
