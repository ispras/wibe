algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  vaeregeneration

pipeline:
  result_path: ./test_results/tests/vaeregeneration
  aggregators:
    - CSV:
        table_name: table
