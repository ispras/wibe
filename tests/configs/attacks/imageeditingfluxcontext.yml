algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  imageeditingfluxcontext:
    num_inference_steps: 1

pipeline:
  result_path: ./test_results/tests/imageeditingfluxcontext
  aggregators:
    - CSV:
        table_name: table
