algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  contrast

pipeline:
  result_path: ./test_results/tests/contrast
  aggregators:
    - CSV:
        table_name: table
