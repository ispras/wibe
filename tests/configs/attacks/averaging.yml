algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  averaging

pipeline:
  result_path: ./test_results/tests/averaging
  aggregators:
    - CSV:
        table_name: table
