algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  randomcropout

pipeline:
  result_path: ./test_results/tests/randomcropout
  aggregators:
    - CSV:
        table_name: table
