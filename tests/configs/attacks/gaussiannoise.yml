algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  gaussiannoise

pipeline:
  result_path: ./test_results/tests/gaussiannoise
  aggregators:
    - CSV:
        table_name: table
