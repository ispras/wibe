algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  adversarialembedding

pipeline:
  result_path: ./test_results/tests/adversarialembedding
  aggregators:
    - CSV:
        table_name: table
