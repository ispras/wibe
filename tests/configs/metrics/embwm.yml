algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  - JPEG:
      quality: 80
  
post_embed_metrics:
  embwm

pipeline:
  result_path: ./test_results/tests/embwm
  aggregators:
    - CSV:
        table_name: table
