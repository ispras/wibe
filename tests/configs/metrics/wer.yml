algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  - JPEG:
      quality: 80
  
post_extract_metrics:
  wer

pipeline:
  result_path: ./test_results/tests/wer
  aggregators:
    - CSV:
        table_name: table
