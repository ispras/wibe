algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  - JPEG:
      quality: 80
  
post_embed_metrics:
  dreamsim

post_attack_metrics:
  dreamsim

pipeline:
  result_path: ./test_results/tests/dreamsim
  aggregators:
    - CSV:
        table_name: table
