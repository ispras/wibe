algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  - JPEG:
      quality: 80
  
post_embed_metrics:
  ssim

post_attack_metrics:
  ssim

pipeline:
  result_path: ./test_results/tests/ssim
  aggregators:
    - CSV:
        table_name: table
