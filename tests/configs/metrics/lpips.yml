algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  - JPEG:
      quality: 80
  
post_embed_metrics:
  lpips

post_attack_metrics:
  lpips

pipeline:
  result_path: ./test_results/tests/lpips
  aggregators:
    - CSV:
        table_name: table
