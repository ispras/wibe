algorithms:
  stable_signature

datasets:
  PromptFolderDataset:
    path: tests/data

attacks:
  - JPEG:
      quality: 80
  
post_embed_metrics:
  clipscore

pipeline:
  result_path: ./test_results/tests/clipscore
  aggregators:
    - CSV:
        table_name: table
