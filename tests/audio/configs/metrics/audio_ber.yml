algorithms:
  FSVC:
    params:
      sample_rate: 16000
      watermark_length: 40
      alpha: 0.04
      gamma1: 0.16
      gamma2: 0.4

post_extract_metrics:
  - BER

datasets:
  - AudioFolderDataset:
      path: tests/audio/data/
      sample_rate: 16000

attacks:
  - WhiteNoise:
      snr_db: 20

pipeline:
  result_path: ./test_results/tests/ber
  aggregators:
    - CSV:
        table_name: table
