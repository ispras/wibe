algorithms:
  cryptomark:
    params:
      profile: light
      target_rate: 16000

datasets:
  - AudioFolderDataset:
      path: tests/audio/data/
      sample_rate: 16000

attacks:
  - Identity

pipeline:
  result_path: ./test_results/tests/cryptomark
  aggregators:
    - CSV:
        table_name: table
