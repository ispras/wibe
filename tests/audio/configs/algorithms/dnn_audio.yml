algorithms:
  dnn_audio:
    params:
      device: cpu

datasets:
  - AudioFolderDataset:
      path: tests/audio/data/
      sample_rate: 16000

attacks:
  - Identity

pipeline:
  result_path: ./test_results/tests/dnn_audio
  aggregators:
    - CSV:
        table_name: table
