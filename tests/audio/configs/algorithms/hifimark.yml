algorithms:
  hifi-mark

datasets:
  - AudioFolderDataset:
      path: tests/audio/data
      sample_rate: 22050

attacks:
  - Identity

pipeline:
  result_path: ./test_results/tests/hifi-mark
  aggregators:
    - CSV:
        table_name: table
