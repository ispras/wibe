algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  - JPEG:
      quality: 80
  
post_extract_metrics:
  extwm

pipeline:
  result_path: ./test_results/tests/extwm
  aggregators:
    - CSV:
        table_name: table
