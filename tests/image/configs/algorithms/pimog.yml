algorithms:
  pimog

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  Identity

pipeline:
  result_path: ./test_results/tests/pimog
  aggregators:
    - CSV:
        table_name: table
