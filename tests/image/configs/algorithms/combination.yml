algorithms:
  - combination:
      algorithms:
        - mbrs
        - videoseal

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  Identity

pipeline:
  result_path: ./test_results/tests/combination
  aggregators:
    - CSV:
        table_name: table
