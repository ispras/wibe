algorithms:
  vine

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  Identity

pipeline:
  result_path: ./test_results/tests/vine
  aggregators:
    - CSV:
        table_name: table
