algorithms:
  ringid

datasets:
  PromptFolderDataset:
    path: tests/image/data

attacks:
  Identity

pipeline:
  result_path: ./test_results/tests/ringid
  aggregators:
    - CSV:
        table_name: table
