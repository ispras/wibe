algorithms:
  maxsive

datasets:
  PromptFolderDataset:
    path: tests/image/data

attacks:
  Identity

pipeline:
  result_path: ./test_results/tests/maxsive
  aggregators:
    - CSV:
        table_name: table
