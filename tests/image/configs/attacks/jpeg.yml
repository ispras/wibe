algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  jpeg

pipeline:
  result_path: ./test_results/tests/jpeg
  aggregators:
    - CSV:
        table_name: table
