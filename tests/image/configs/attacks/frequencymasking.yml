algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  frequencymasking

pipeline:
  result_path: ./test_results/tests/frequencymasking
  aggregators:
    - CSV:
        table_name: table
