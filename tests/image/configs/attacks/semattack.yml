algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  semattack

pipeline:
  result_path: ./test_results/tests/semattack
  aggregators:
    - CSV:
        table_name: table
