algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  colorinversion

pipeline:
  result_path: ./test_results/tests/colorinversion
  aggregators:
    - CSV:
        table_name: table
