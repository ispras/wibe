algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/data

attacks:
  diffusionregeneration

pipeline:
  result_path: ./test_results/tests/diffusionregeneration
  aggregators:
    - CSV:
        table_name: table
