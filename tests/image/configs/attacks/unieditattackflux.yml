algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  UniEditAttackFlux

pipeline:
  result_path: ./test_results/tests/unieditattackflux
  aggregators:
    - CSV:
        table_name: table
