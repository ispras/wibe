algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  - randomcrop
pipeline:
  result_path: ./test_results/tests/randomcrop
  aggregators:
    - CSV:
        table_name: table
