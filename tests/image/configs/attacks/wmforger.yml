algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  wmforger

pipeline:
  result_path: ./test_results/tests/wmforger
  aggregators:
    - CSV:
        table_name: table
