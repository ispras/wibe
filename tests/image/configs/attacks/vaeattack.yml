algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  vaeattack

pipeline:
  result_path: ./test_results/tests/vaeattack
  aggregators:
    - CSV:
        table_name: table
