algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  imagewatermark

pipeline:
  result_path: ./test_results/tests/imagewatermark
  aggregators:
    - CSV:
        table_name: table
