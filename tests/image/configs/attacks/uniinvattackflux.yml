algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  UniInvAttackFlux

pipeline:
  result_path: ./test_results/tests/uniinvattackflux
  aggregators:
    - CSV:
        table_name: table
