algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  centercrop

pipeline:
  result_path: ./test_results/tests/centercrop
  aggregators:
    - CSV:
        table_name: table
