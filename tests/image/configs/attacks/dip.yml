algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  dip

pipeline:
  result_path: ./test_results/tests/dip
  aggregators:
    - CSV:
        table_name: table
