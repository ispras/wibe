algorithms:
  dct_marker

datasets:
  ImageFolderDataset:
    path: tests/image/data

attacks:
  - rotate
pipeline:
  result_path: ./test_results/tests/rotate
  aggregators:
    - CSV:
        table_name: table
